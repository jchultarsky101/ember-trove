/// Visual knowledge graph (SVG node-link diagram).
///
/// Phase 1 stub — Phase 4 renders the actual graph.
use leptos::prelude::*;

#[component]
pub fn GraphView() -> impl IntoView {
    view! {
        <div class="flex items-center justify-center h-full text-gray-400 dark:text-gray-600">
            <p class="text-sm">"Graph view — coming in Phase 4."</p>
        </div>
    }
}
